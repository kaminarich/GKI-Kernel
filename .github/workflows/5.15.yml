name: Build GKI 5.15 (Image Only)

on:
  workflow_dispatch:
    inputs:
      target_version:
        description: 'Target Version (Commit Hash / Tag / Branch)'
        default: 'android12-5.15'
        required: true
      kernel_name:
        description: 'Kernel Name (Optional)'
        required: false
        default: ''

jobs:
  build:
    name: Build Kernel 5.15
    runs-on: ubuntu-22.04

    steps:
      - name: Maximize Build Space
        uses: easimon/maximize-build-space@master
        with:
          root-reserve-mb: 8192
          swap-size-mb: 10240
          remove-dotnet: 'true'
          remove-android: 'true'
          remove-codeql: 'true'

      - name: Install Dependencies
        run: |
          sudo apt update
          sudo apt install -y git curl build-essential bc bison flex libssl-dev \
            libelf-dev libncurses-dev lld python3

      - name: Clone Kernel Source (5.15)
        run: |
          echo "Cloning GKI 5.15..."
          # Clone depth 1 agar cepat
          git clone https://android.googlesource.com/kernel/common -b android12-5.15 --depth 1 source
          cd source
          # Fetch sedikit history agar bisa checkout commit tertentu jika perlu
          git fetch --unshallow || true

      - name: Setup Clang (Android 12/5.15 Standard)
        run: |
          mkdir -p toolchain/clang
          cd toolchain/clang
          echo "Downloading Clang r416183b (Standard for GKI 5.15)..."
          # Menggunakan Clang official Google untuk Android 12
          curl -L "https://android.googlesource.com/platform/prebuilts/clang/host/linux-x86/+archive/refs/tags/android-12.1.0_r27/clang-r416183b1.tar.gz" | tar -xz
          
      - name: Checkout Target Version
        run: |
          cd source
          TARGET="${{ github.event.inputs.target_version }}"
          echo "Checking out: $TARGET"
          
          # Jika gagal checkout (misal input branch/tag salah), lanjut ke latest
          git checkout $TARGET || echo "Target not found, staying on latest commit."

      - name: Apply SukiSU & SUSFS Patches
        run: |
          cd source
          echo "Downloading SukiSU Setup..."
          curl -LSs "https://raw.githubusercontent.com/SukiSU-Ultra/SukiSU-Ultra/main/kernel/setup.sh" | bash -s main
          curl -LSs "https://raw.githubusercontent.com/SukiSU-Ultra/SukiSU-Ultra/main/kernel/setup.sh" | bash -s susfs-main
          
          echo "Cloning SUSFS 5.15 patches..."
          git clone https://gitlab.com/simonpunk/susfs4ksu/ -b gki-android12-5.15 sus_temp
          
          # Copy file patch SUSFS
          cp -r sus_temp/kernel_patches/fs/* fs/
          cp -r sus_temp/kernel_patches/include/* include/
          cp sus_temp/kernel_patches/50_add_susfs_in_gki-android12-5.15.patch .
          
          echo "Applying SUSFS Patch..."
          patch -p1 < 50_add_susfs_in_gki-android12-5.15.patch
          
          rm -rf sus_temp

      - name: Fix task_mmu.c (SukiSU Fix)
        run: |
          cd source
          # Fix sederhana untuk mencegah error show_pad
          sed -i '/show_pad:/d' fs/proc/task_mmu.c
          sed -i '1ishow_pad:' fs/proc/task_mmu.c

      - name: Configure Kernel
        run: |
          # Set Environment Variables
          export PATH=$GITHUB_WORKSPACE/toolchain/clang/bin:$PATH
          export ARCH=arm64
          export CLANG_TRIPLE=aarch64-linux-gnu-
          export CROSS_COMPILE=aarch64-linux-gnu-
          export CROSS_COMPILE_COMPAT=arm-linux-gnueabi-
          export LLVM=1
          
          cd source
          
          echo "Generating GKI Defconfig..."
          make O=out gki_defconfig
          
          echo "Modifying Config (LTO, KPM, HZ)..."
          # Mengaktifkan ThinLTO (Performance)
          scripts/config --file out/.config \
            -e LTO_CLANG \
            -d LTO_NONE \
            -e LTO_CLANG_THIN \
            -d LTO_CLANG_FULL \
            -e THINLTO
            
          # Mengaktifkan KPM & KSU Settings
          scripts/config --file out/.config \
            -e KPM \
            -e CONFIG_KSU \
            -e CONFIG_KSU_SUSFS \
            -e CONFIG_KSU_SUSFS_SUS_PATH \
            -e CONFIG_KSU_SUSFS_SUS_MOUNT
            
          # Set HZ ke 300 (Wajib untuk GKI)
          scripts/config --file out/.config --set-val CONFIG_HZ 300
          
          # Set Custom Name
          NAME="${{ github.event.inputs.kernel_name }}"
          if [ ! -z "$NAME" ]; then
             sed -i "s/^EXTRAVERSION =.*/EXTRAVERSION = -$NAME/" Makefile
          fi

      - name: Build Kernel Image
        run: |
          export PATH=$GITHUB_WORKSPACE/toolchain/clang/bin:$PATH
          export ARCH=arm64
          export LLVM=1
          
          cd source
          echo "Building Image..."
          
          make -j$(nproc --all) \
              O=out \
              CC=clang \
              LD=ld.lld \
              AR=llvm-ar \
              NM=llvm-nm \
              OBJCOPY=llvm-objcopy \
              OBJDUMP=llvm-objdump \
              STRIP=llvm-strip \
              Image

      - name: Apply KPM Binary Patch
        run: |
          cd source/out/arch/arm64/boot
          
          if [ ! -f Image ]; then
             echo "::error::Build Failed! Image not found."
             exit 1
          fi
          
          echo "Applying KPM Patch..."
          curl -LSs "https://github.com/SukiSU-Ultra/SukiSU_patch/raw/refs/heads/main/kpm/patch_linux" -o patch
          chmod +x patch
          ./patch
          
          # Rename oImage (patched) to Image
          if [ -f oImage ]; then
             mv -f oImage Image
             echo "KPM Patch Applied Successfully."
          else
             echo "::error::KPM Patch Failed (oImage not created)."
             exit 1
          fi

      - name: Upload Image to Artifacts
        uses: actions/upload-artifact@v4
        with:
          name: Image-GKI-5.15
          path: source/out/arch/arm64/boot/Image

      - name: Upload Image to PixelDrain
        run: |
          cd source/out/arch/arm64/boot
          echo "Uploading Image to PixelDrain..."
          curl -T Image -u :23c84c82-ce06-4e35-bc1f-69eaa13b9958 https://pixeldrain.com/api/file
