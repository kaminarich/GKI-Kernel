Name: KowSU 3

on:
  workflow_dispatch:
    inputs:
      kernel_name:
        description: 'Nama Kernel (Opsional)'
        required: false
        default: ''
      clang_version:
        description: 'Versi Clang'
        required: true
        type: choice
        options:
          - 'r536225 (Clang 19)'
          - 'r547379 (Clang 20)'
        default: 'r547379 (Clang 20)'

jobs:
  build:
    name: Build GKI - ${{ github.event.inputs.clang_version }}
    runs-on: ubuntu-latest
    
    steps:
      - name: Cleanup & Maximize Space
        uses: easimon/maximize-build-space@master
        with:
          root-reserve-mb: 8192
          swap-size-mb: 4096
          remove-dotnet: 'true'
          remove-android: 'true'
          remove-haskell: 'true'
          remove-codeql: 'true'

      - name: Install Dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y git curl repo build-essential bc bison flex libssl-dev \
            libelf-dev python3 python3-pip unzip zip rsync libncurses-dev lld ccache \
            cmake ninja-build

      - name: Configure Git
        run: |
          git config --global user.name "GitHub Action"
          git config --global user.email "action@github.com"

      - name: Initialize Repo & Sync
        run: |
          mkdir -p gki
          cd gki
          repo init --depth=1 -u https://android.googlesource.com/kernel/manifest -b common-android12-5.10
          repo sync -c -j$(nproc --all) --no-tags --fail-fast

      - name: Setup Clang
        working-directory: gki
        run: |
          mkdir -p prebuilts-master/clang/host/linux-x86
          
          if [ "${{ github.event.inputs.clang_version }}" == "r536225 (Clang 19)" ]; then
            echo "Setting up Clang-19 (r536225)..."
            CLANG_VER="clang-r536225"
            CLANG_URL="https://android.googlesource.com/platform/prebuilts/clang/host/linux-x86/+archive/refs/heads/main-kernel-2025/clang-r536225.tar.gz"
          else
            echo "Setting up Clang-20 (r547379)..."
            CLANG_VER="clang-r547379"
            CLANG_URL="https://android.googlesource.com/platform/prebuilts/clang/host/linux-x86/+archive/62cdcefa89e31af2d72c366e8b5ef8db84caea62/clang-r547379.tar.gz"
          fi
          
          echo "CLANG_VER=$CLANG_VER" >> $GITHUB_ENV

          mkdir -p prebuilts-master/clang/host/linux-x86/$CLANG_VER
          cd prebuilts-master/clang/host/linux-x86
          curl -L "${CLANG_URL}" | tar -xz -C $CLANG_VER
          
          # Fix Symlink for build script compatibility
          rm -rf clang-r416183b
          ln -sf $CLANG_VER clang-r416183b

      - name: Setup Upstream & KernelSU-KowSU (SUBMODULE METHOD)
        working-directory: gki/common
        run: |
          # Setup Remote Upstream (Rama Bondan)
          git remote add rama https://github.com/ramabondanp/android_kernel_common-5.10.git
          git fetch -j12 rama
          
          # Checkout branch staging
          git checkout -b update-5.10.247 rama/android12-5.10-staging

          # INIT & UPDATE SUBMODULE
          echo "Updating KernelSU submodule..."
          git submodule update --init --recursive drivers/staging/kernelsu

          echo "✅ KernelSU submodule updated"

      - name: Apply Patches
        working-directory: gki/common
        run: |
          echo "--- Starting Kernel Source Patching ---"
          
          # Xiaomi Compatibility Patches
          sed -i '/pr_warn.*disagrees about version of symbol.*/,+1 s/.*/return 1;/' kernel/module.c
          sed -i '/^static int drm_atomic_check_valid_clones/,/^}/d' drivers/gpu/drm/drm_atomic_helper.c
          sed -i '/ret = drm_atomic_check_valid_clones/,/return ret;/d' drivers/gpu/drm/drm_atomic_helper.c
          echo "✅ Patch: Xiaomi compatibility applied."
          
          # --- PERFORMANCE SOURCE TWEAKS (ROBUST REGEX METHOD) ---
          
          # 1. Set Default Swappiness to 100 (Aggressive Swap)
          # Menggunakan regex [0-9]* agar angka berapapun yg ada diganti jadi 100
          sed -i 's/int vm_swappiness = [0-9]*;/int vm_swappiness = 100;/g' mm/vmscan.c
          echo "✅ Tweak: vm_swappiness patched to 100 (using regex)."

          # 2. Retain VFS Cache Longer (vfs_cache_pressure = 50)
          # Menggunakan regex [0-9]* agar angka berapapun yg ada diganti jadi 50
          sed -i 's/int sysctl_vfs_cache_pressure = [0-9]*;/int sysctl_vfs_cache_pressure = 50;/g' fs/dcache.c 2>/dev/null || true
          echo "✅ Tweak: vfs_cache_pressure patched to 50 (using regex)."

      - name: Configure Defconfig (Optimized)
        working-directory: gki/common
        run: |
          DEFCONFIG="arch/arm64/configs/gki_defconfig"
          
          cat <<EOF >> "$DEFCONFIG"
          # --- KernelSU & General Setup ---
          CONFIG_KSU=y
          CONFIG_KPM=n
          CONFIG_KSU_MANUAL_HOOK=y
          CONFIG_KSU_KPROBES_HOOK=n
          CONFIG_SWAP=y
          CONFIG_ZRAM=y
          CONFIG_ZRAM_WRITEBACK=y
          CONFIG_ZRAM_DEF_COMP_LZ4=y 

          # --- SUSFS Configuration ---
          CONFIG_KSU_SUSFS=y
          CONFIG_KSU_SUSFS_HAS_MAGIC_MOUNT=y
          CONFIG_KSU_SUSFS_SUS_PATH=y
          CONFIG_KSU_SUSFS_SUS_MOUNT=y
          CONFIG_KSU_SUSFS_AUTO_ADD_SUS_KSU_DEFAULT_MOUNT=y
          CONFIG_KSU_SUSFS_AUTO_ADD_SUS_BIND_MOUNT=y
          CONFIG_KSU_SUSFS_SUS_KSTAT=y
          CONFIG_KSU_SUSFS_SUS_OVERLAYFS=y
          CONFIG_KSU_SUSFS_TRY_UMOUNT=y
          CONFIG_KSU_SUSFS_AUTO_ADD_TRY_UMOUNT_FOR_BIND_MOUNT=y
          CONFIG_KSU_SUSFS_SPOOF_UNAME=y
          CONFIG_KSU_SUSFS_ENABLE_LOG=y
          CONFIG_KSU_SUSFS_HIDE_KSU_SUSFS_SYMBOLS=y
          CONFIG_KSU_SUSFS_SPOOF_CMDLINE_OR_BOOTCONFIG=y
          CONFIG_KSU_SUSFS_OPEN_REDIRECT=y
          CONFIG_KSU_SUSFS_SUS_SU=y
          CONFIG_KSU_SUSFS_SUS_KSTAT_SPOOF_GENERIC=y
          CONFIG_KSU_SUSFS_AUTO_ADD_SUS_KSTAT=y

          # --- PERFORMANCE & EFFICIENCY OPTIMIZATIONS ---
          
          # 1. Reduce Overhead (Disable Debugging & Logging)
          CONFIG_PRINTK_TIME=n
          CONFIG_DYNAMIC_DEBUG=n
          CONFIG_DEBUG_INFO=n
          CONFIG_DEBUG_FS=n
          CONFIG_DEBUG_KERNEL=n
          CONFIG_SLUB_DEBUG=n
          CONFIG_FRAME_POINTER=n

          # 2. CPU Scheduling & Latency (UI Responsiveness & Gaming)
          CONFIG_HZ_300=y
          CONFIG_HZ=300
          CONFIG_SCHED_SMT=y
          CONFIG_SCHED_MC=y
          CONFIG_UCLAMP_TASK=y
          CONFIG_ENERGY_MODEL=y
          CONFIG_CPU_FREQ_GOV_SCHEDUTIL=y
          CONFIG_CPU_FREQ_DEFAULT_GOV_SCHEDUTIL=y
          CONFIG_PREEMPT=y
          CONFIG_PREEMPT_VOLUNTARY=n
          CONFIG_PREEMPT_NONE=n
          
          # 3. Timer Optimization (Power Saving & Real-time)
          CONFIG_NO_HZ_FULL=y
          CONFIG_NO_HZ=y
          CONFIG_HIGH_RES_TIMERS=y
          CONFIG_RCU_NOCB_CPU=y
          
          # 4. Memory Management (Disable Compaction = Save CPU Cycles)
          CONFIG_COMPACTION=n
          CONFIG_MIGRATION=n
          CONFIG_KSM=n
          CONFIG_PAGE_POISONING=n
          CONFIG_VM_EVENT_COUNTS=n
          CONFIG_CLEANCACHE=y
          CONFIG_FRONTSWAP=y
          
          # 5. Network (TCP Optimization & Lower Latency)
          CONFIG_TCP_CONG_ADVANCED=y
          CONFIG_TCP_CONG_BBR=y
          CONFIG_DEFAULT_BBR=y
          CONFIG_NET_SCH_FQ=y
          CONFIG_TCP_TIMESTAMPS=n
          CONFIG_TCP_NODELAY=y
          CONFIG_IP_NF_TARGET_TTL=y
          CONFIG_IP6_NF_TARGET_HL=y
          
          # 6. Filesystem
          CONFIG_TMPFS_XATTR=y
          CONFIG_TMPFS_POSIX_ACL=y
          CONFIG_FILE_LOCKING=y
          EOF
          echo "✅ Defconfig updated with all optimizations."

      - name: Apply Cherry-Picks and Check Status
        working-directory: gki/common
        run: |
          echo "--- Starting Cherry-Pick Patches ---"
          
          # Patch 1: Common 5.10 update/fix
          if curl -L "https://github.com/ramabondanp/android_kernel_common-5.10/commit/4fe04b60009e.patch" | git am -3; then
             echo "✅ Patch 1 (4fe04b60009e) applied via git am."
          else
             echo "⚠️ Patch 1 (4fe04b60009e) git am failed, falling back to patch..."
             if curl -L "https://github.com/ramabondanp/android_kernel_common-5.10/commit/4fe04b60009e.patch" | patch -p1; then
                echo "✅ Patch 1 applied successfully via 'patch' fallback."
             else
                echo "❌ FAILED: Patch 1 application failed completely! Exiting..."
                exit 1
             fi
          fi

          # Patch 2: Xiaomi Marble specific fix
          if curl -L "https://github.com/Pzqqt/android_kernel_xiaomi_marble/commit/477706c2bcaaf48350c61f9f6824e1e454617147.patch" | git am -3; then
             echo "✅ Patch 2 (477706c2bcaaf48350c61f9f6824e1e454617147) applied via git am."
          else
             echo "⚠️ Patch 2 (477706c2bcaaf48350c61f9f6824e1e454617147) git am failed, falling back to patch..."
             if curl -L "https://github.com/Pzqqt/android_kernel_xiaomi_marble/commit/477706c2bcaaf48350c61f9f6824e1e454617147.patch" | patch -p1; then
                echo "✅ Patch 2 applied successfully via 'patch' fallback."
             else
                echo "❌ FAILED: Patch 2 application failed completely! Exiting..."
                exit 1
             fi
          fi

      - name: Final Build Config & Version Fix
        working-directory: gki
        run: |
          # 1. FIX: Hapus semua file localversion dari upstream agar tidak bentrok
          rm -f common/localversion*
          
          # 2. Update build config untuk Clang version
          sed -i "s/clang-r[0-9]*[a-z]*/${CLANG_VER}/g" common/build.config.gki.aarch64
          sed -i "s/clang-r[0-9]*[a-z]*/${CLANG_VER}/g" common/build.config.common 2>/dev/null || true
          
          # 3. Disable check_defconfig
          sed -i 's/check_defconfig//' ./common/build.config.gki
          
          # 4. Handle Kernel Name / Branding
          DEFCONFIG2="./common/arch/arm64/configs/gki_defconfig"
          
          # Hapus settingan lama
          sed -i '/^CONFIG_LOCALVERSION=/d' "$DEFCONFIG2"
          
          if [ ! -z "${{ github.event.inputs.kernel_name }}" ]; then
            echo "Using custom kernel name: ${{ github.event.inputs.kernel_name }}"
            BRAND="-${{ github.event.inputs.kernel_name }}"
          else
            BRAND=""
          fi
          
          # FIX: Paksa script setlocalversion untuk HANYA output brand kita
          echo "echo \"$BRAND\"" > common/scripts/setlocalversion
          chmod +x common/scripts/setlocalversion
          
          echo "CONFIG_LOCALVERSION=\"$BRAND\"" >> "$DEFCONFIG2"
          echo "CONFIG_LOCALVERSION_AUTO=n" >> "$DEFCONFIG2"

      - name: Build Kernel
        working-directory: gki
        run: |
          echo "--- Starting Kernel Build ---"
          export CC="ccache clang"
          export USE_CCACHE=1
          export CCACHE_DIR="/tmp/ccache"
          ccache -M 5G
          
          echo "Starting build with LTO=thin..."
          LTO=thin BUILD_CONFIG=common/build.config.gki.aarch64 build/build.sh -j$(nproc)
          echo "✅ Build command executed. Check logs above for final compilation status."

      - name: Upload Image
        uses: actions/upload-artifact@v4
        with:
          name: Image${{ github.event.inputs.kernel_name }}
          path: gki/out/**/dist/Image
          
